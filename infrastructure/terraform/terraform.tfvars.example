# Copy to terraform.tfvars and fill in real values. terraform.tfvars is gitignored —
# never commit it. Values here mirror .env.example's OCI_* fields.

tenancy_ocid     = "ocid1.tenancy.oc1..exampleuniqueID"
user_ocid        = "ocid1.user.oc1..exampleuniqueID"
fingerprint      = "aa:bb:cc:dd:ee:ff:00:11:22:33:44:55:66:77:88:99"
private_key_path = "~/.oci/oci_api_key.pem"
region           = "us-ashburn-1"
compartment_ocid = "ocid1.tenancy.oc1..exampleuniqueID" # root compartment OCID == tenancy OCID

# Set this to your own IP, e.g. "203.0.113.4/32" — never leave it as 0.0.0.0/0.
admin_ssh_cidr = "203.0.113.4/32"

# Public half of a dedicated admin keypair (e.g. `ssh-keygen -t ed25519 -f
# ~/.oci/palworld-admin-key`) — separate from both the OCI API key and the bot's
# forced-command key. This one gets a real shell on both VMs.
admin_ssh_public_key = "ssh-ed25519 AAAA...REPLACE_WITH_REAL_PUBLIC_KEY... palworld-admin"

# vcn_cidr, subnet_cidr, palworld_port, rcon_port all have sensible defaults (see
# variables.tf) — only override if you need something non-standard.

# Public half of the dedicated keypair from `ssh-keygen -t ed25519 -f palworld-bot-key`
# (see infrastructure/cloud-init/README.md for the full generation/injection flow).
palworld_bot_ssh_public_key = "ssh-ed25519 AAAA...REPLACE_WITH_REAL_PUBLIC_KEY... palworld-discord-bot"
